# 如何评审与改进 Skill

每次修改先说明一个具体用户任务，再判断它是否属于现有 Skill。一个 Skill 应当解决稳定、可重复的工作流，不能只保存一段很长的提示词。

## 七项评审

| 项目 | 要检查的问题 | 通过标准 |
|---|---|---|
| 触发 | 什么请求会调用它？什么相近请求不应调用？ | `description` 同时写清正向场景和边界 |
| 输入 | 完成任务必须有哪些材料？ | 缺少关键参考图时会追问，不会假装看过 |
| 流程 | 步骤是否能让不同用户重复执行？ | 步骤具体、顺序合理，不依赖隐含上下文 |
| 工具 | 宿主是否真的提供文中所说的能力？ | 不伪造模型选择、蒙版、透明通道或批量能力 |
| 约束 | 编辑时哪些内容必须保持不变？ | 主体、文字、数量、构图等不变项被明确记录 |
| 验收 | 如何判断输出合格？ | 有文字、数量、裁切、结构和参考一致性的检查 |
| 分发 | 新用户能否安装、理解并移除？ | 校验通过，README 命令可用，卸载只影响本包目录 |

## 最小测试集

每个 Skill 至少准备四类请求：

1. **典型命中**：明确属于该 Skill 的常见任务。
2. **自然语言命中**：用户不说 Skill 名称，但描述满足触发条件。
3. **不应命中**：相近却属于其他工具的任务，例如精确数据图应转到确定性绘图。
4. **能力边界**：请求宿主未暴露的参数时，应说明限制并使用实际可用能力。

评审结果记录“输入、预期路由、预期行为、实际行为、是否通过”。不要只检查文档能否解析；真正重要的是路由和最终行为。

## 提交前

运行：

```bash
./scripts/validate.sh
```

再从一个临时 HOME 测试安装、重复安装和卸载，确认只出现六个预期 Skill，旧版本进入备份目录。若更改模型说明，请同时核对 OpenAI 当前文档，并在 Skill 中避免把可能变化的价格、速度或产品支持范围写成永久事实。
